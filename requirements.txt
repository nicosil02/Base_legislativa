requests>=2.31
pycryptodome>=3.20
openpyxl>=3.1
streamlit>=1.34
pandas>=2.0
streamlit-aggrid>=0.3.4
# Clasificador ML para temas de PLs (modulo clasificador/):
# TF-IDF + LogisticRegression, entrenado con 14,589 PLs etiquetados a mano.
# scikit-learn pesa ~12 MB instalado, joblib persiste el modelo en ~2 MB.
scikit-learn>=1.4
joblib>=1.3
# Ecuador-only: usado por scraper_ec.playwright_detail para extraer URLs
# públicas de PDFs desde el portal Ppless v2 (Angular SPA).
# Requiere también `python -m playwright install chromium` después del pip install.
playwright>=1.59
# Cookies para persistir sesion: implementado via JS inyectado en
# components.html (iframe srcdoc same-origin) + lectura via st.context.cookies.
# No requiere libreria externa.
cloudscraper
# noticias.registro_oficial_ec: lee el PDF real de cada edicion (Corte
# Constitucional EC) en vez de solo el indice en texto del post WP.
# PyMuPDF extrae texto Y renderiza paginas a imagen sin depender de Poppler
# (binario del sistema) - importa como `fitz`. pytesseract hace OCR de las
# paginas que PyMuPDF no puede leer (escaneadas); requiere el binario
# `tesseract-ocr` instalado aparte (apt-get en CI, no viene con pip).
pymupdf>=1.24
pytesseract>=0.3
pillow>=10.0
